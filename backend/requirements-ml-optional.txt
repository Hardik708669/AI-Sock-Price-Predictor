# Optional advanced ML dependencies.
# Install only when you want the native accelerated engines.
#
# Windows note:
# TA-Lib often requires a prebuilt wheel or local C library installation.
# The AI engine has deterministic fallbacks when these packages are absent.

tensorflow==2.18.0
prophet==1.1.6
TA-Lib==0.4.32
